// CODE LEARNER · Lesson Data v2
// timestamp: { time, label, content, bullets, snippet }
// codePanel: { explanation, lines:[{comment,code}] }
// challenge type: 'code' | 'mcq' | 'blank'

export const LANGUAGES = [
  { id:'python',     name:'Python',         icon:'🐍', color:'#3776AB', tagline:'Beginner-friendly · AI · Data Science · Backend', totalXP:1200 },
  { id:'javascript', name:'JavaScript',     icon:'⚡', color:'#F7DF1E', tagline:'Web Interactivity · Frontend · Node.js · APIs',    totalXP:1200 },
  { id:'htmlcss',    name:'HTML & CSS',     icon:'🎨', color:'#E34F26', tagline:'Web Structure · Styling · Responsive Design',      totalXP:1200 },
  { id:'fullstack',  name:'Full Stack Web', icon:'🌐', color:'#61DAFB', tagline:'React · Node.js · Databases · Deployment',         totalXP:1200 },
];

// ─── PYTHON ──────────────────────────────
export const pythonLessons = {
  beginner: [
    {
      id:'py-b-1', title:'Welcome to Python', xp:40,
      timestamps:[
        { time:'0:00', label:'What is Python?', content:'Python is a high-level, general-purpose language created by Guido van Rossum in 1991. Its core design goal is readability — Python code reads almost like plain English.', bullets:['Runs on Windows, Mac, Linux and the web','Free and open-source forever','One of the top 3 most-used languages in the world'], snippet:null },
        { time:'1:20', label:'Why learn Python?', content:'Python is used across almost every field in tech. Whether you want to build websites, train AI models, analyse data or automate tasks — Python does it all with less code than other languages.', bullets:['Web backends: Instagram, YouTube, Reddit all run Python','AI & ML: NumPy, TensorFlow, PyTorch are Python libraries','Automation: replace hours of manual work with 10 lines of script'], snippet:null },
        { time:'2:45', label:'Installing & running Python', content:'Download Python from python.org. During install, tick "Add Python to PATH". Then open your terminal and type python3 to confirm it works.', bullets:['python3 --version  →  confirms install succeeded','python3 filename.py  →  runs a script file','Recommended editor: VS Code with the Python extension'], snippet:null },
        { time:'4:10', label:'The print() function', content:'print() is your first Python function. It sends output to the terminal screen. You will use it in virtually every program you write — for output, debugging, and checking values.', bullets:['Wrap text in quotes: print("Hello")','Print numbers directly: print(42)','Print multiple values: print("Age:", 20)  ← comma separates them'], snippet:'print("Hello, World!")\nprint("Welcome to Python!")\nprint("My age is", 20)' },
      ],
      codePanel:{
        explanation:'Every Python program starts with print() to display output. These three lines each show something different — text, a number, and a mix of both. The comment on each line explains exactly what it does.',
        lines:[
          { comment:'Display a text message — text must be in quotes', code:'print("Hello, World!")' },
          { comment:'Print a number — no quotes needed', code:'print(42)' },
          { comment:'Mix text and a number — comma adds a space between them', code:'print("My age is", 20)' },
        ],
      },
      quiz:{ question:'Which function displays output on the screen in Python?', options:['show()','display()','print()','output()'], answer:2, explanation:'print() is Python\'s built-in output function. It\'s one of the very first things every Python developer learns and uses every single day.' },
      challenge:{ type:'code', title:'Your First Python Program', task:'A user named "Alice" is 11 years old. Print her name and age in exactly 1 sentence:\nAlice is 11 years old.', expectedOutput:'Alice is 11 years old.', hint:'Use print() with the full sentence in quotes.', starterCode:'# Write your solution below\n', validate:(output) => output.trim() === 'Alice is 11 years old.' },
    },
    {
      id:'py-b-2', title:'Variables & Data Types', xp:40,
      timestamps:[
        { time:'0:00', label:'What is a variable?', content:'A variable is a named container that stores a value in memory. Think of it like a labelled box — you put something inside, give it a name, and retrieve it anytime by that name.', bullets:['Created instantly: just write name = value','No type declaration needed — Python figures it out','Variable names are case-sensitive: age ≠ Age'], snippet:'name = "Alice"\nage = 25\nprint(name, age)' },
        { time:'1:30', label:'Strings — storing text', content:'A string (str) stores text. Always wrap it in single or double quotes. Strings can hold letters, numbers, spaces and symbols — anything inside quotes is text, not a number.', bullets:['Single or double quotes both work: "hello" or \'hello\'','len("hello") gives you 5 — the character count','str(42) converts the number 42 into the text "42"'], snippet:'name = "Alice"\nprint(type(name))   # <class \'str\'>\nprint(len(name))     # 5' },
        { time:'3:00', label:'Integers & Floats', content:'int stores whole numbers (no decimal). float stores decimal numbers. Python automatically picks the right one — you never have to specify which.', bullets:['42 → int  |  3.14 → float','Maths works directly on both types','int(3.9) gives 3 — chops the decimal, does NOT round'], snippet:'age = 25          # int\nheight = 5.7       # float\nprint(type(age))   # <class \'int\'>\nprint(type(height)) # <class \'float\'>' },
        { time:'5:15', label:'Booleans', content:'A boolean (bool) has exactly two values: True or False. These power every condition, every if-statement and every loop in your programs. The capital T and F are required in Python.', bullets:['True and False must be capitalised exactly','bool(0) → False  |  bool(1) → True','Used in every if-statement and while-loop you will ever write'], snippet:'is_student = True\nhas_job = False\nprint(type(is_student))  # <class \'bool\'>\nprint(is_student)         # True' },
        { time:'6:40', label:'Checking types with type()', content:'The type() function tells you exactly what kind of data a variable holds. Essential for debugging when you\'re unsure why something isn\'t working as expected.', bullets:['type(42) → <class \'int\'>','type("hi") → <class \'str\'>','type(True) → <class \'bool\'>'], snippet:'x = "hello"\nprint(type(x))      # <class \'str\'>\nx = 99\nprint(type(x))      # <class \'int\'>' },
      ],
      codePanel:{
        explanation:'Variables hold different kinds of data. Each line below creates one variable of a different type and prints it with a label — read the comment to see exactly what each line stores.',
        lines:[
          { comment:'str — text, always in quotes', code:'name = "Alice"' },
          { comment:'int — whole number, no quotes', code:'age = 25' },
          { comment:'float — decimal number', code:'height = 5.7' },
          { comment:'bool — only True or False (capital T/F)', code:'is_student = True' },
          { comment:'Print all four with labels', code:'print("Name:", name, "| Age:", age)' },
        ],
      },
      quiz:{ question:'What data type is the value True in Python?', options:['str','int','float','bool'], answer:3, explanation:'True and False are boolean (bool) values. They must be capitalised in Python — lowercase true would cause a NameError.' },
      challenge:{ type:'code', title:'Personal Profile', task:'Create 3 variables: name (any string), age (any integer), is_student (True or False).\nThen print all three on ONE line.\nExample output: Alice 20 True', expectedOutput:null, hint:'Use print(name, age, is_student) — commas print all three with spaces.', starterCode:'name = \nage = \nis_student = \n\nprint(name, age, is_student)\n', validate:(output) => { const t=output.trim(); return t.length>0 && (t.includes('True')||t.includes('False')); } },
    },
    {
      id:'py-b-3', title:'Operators & Expressions', xp:40,
      timestamps:[
        { time:'0:00', label:'Arithmetic operators', content:'Arithmetic operators perform maths. Python follows standard order of operations — brackets first, then powers, then multiply/divide, then add/subtract.', bullets:['+ add  |  - subtract  |  * multiply','/ always returns a float: 10/2 → 5.0','Use brackets to control order: (2+3)*4 = 20'], snippet:'print(10 + 3)   # 13\nprint(10 - 3)   # 7\nprint(10 * 3)   # 30\nprint(10 / 3)   # 3.333...' },
        { time:'2:10', label:'Floor division and modulus', content:'// divides and rounds DOWN to the nearest whole number. % returns the remainder after division — extremely useful for checking even/odd and cycling through values.', bullets:['10 // 3 = 3  (not 3.33 — truncates decimal)','10 % 3 = 1  (10 ÷ 3 leaves remainder 1)','n % 2 == 0 means n is even — you\'ll use this constantly'], snippet:'print(10 // 3)  # 3  floor division\nprint(10 % 3)   # 1  remainder\nprint(7 % 2)    # 1  odd\nprint(8 % 2)    # 0  even' },
        { time:'3:45', label:'Power operator **', content:'** raises a number to a power. It\'s cleaner than calling a function and works with integers and floats — including fractional powers like square root (0.5).', bullets:['2 ** 10 = 1024  (2 to the power of 10)','9 ** 0.5 = 3.0  (square root)','** has higher priority than * and /'], snippet:'print(2 ** 8)    # 256\nprint(3 ** 3)    # 27\nprint(9 ** 0.5)  # 3.0' },
        { time:'5:00', label:'Comparison operators', content:'Comparison operators compare two values and always return True or False. These are the backbone of every decision (if-statement) in your programs.', bullets:['== checks equality — two equals signs, NOT one','!= means "not equal to"','> < >= <= work exactly like in maths'], snippet:'print(10 > 5)    # True\nprint(10 == 10)  # True\nprint(10 != 5)   # True\nprint(3 >= 3)    # True' },
        { time:'6:30', label:'Logical operators', content:'Logical operators combine multiple conditions. and requires BOTH to be True. or requires at least ONE to be True. not flips True to False and vice versa.', bullets:['True and False → False  (both must be True)','True or False  → True   (one is enough)','not True       → False  (flips the value)'], snippet:'age = 20\nhas_id = True\nprint(age >= 18 and has_id)  # True\nprint(age < 18 or has_id)    # True\nprint(not has_id)             # False' },
      ],
      codePanel:{
        explanation:'Each operator does something distinct. These six lines demonstrate the most important ones — read each comment BEFORE running to predict the output, then verify.',
        lines:[
          { comment:'Basic addition — result is 13', code:'print(10 + 3)' },
          { comment:'Floor division — cuts decimal, gives 3', code:'print(10 // 3)' },
          { comment:'Modulus — remainder after dividing, gives 1', code:'print(10 % 3)' },
          { comment:'Power — 2 to the 8th = 256', code:'print(2 ** 8)' },
          { comment:'Comparison — returns True or False', code:'print(10 > 5)' },
          { comment:'Logical — both must be True', code:'print(10 > 5 and 3 < 10)' },
        ],
      },
      quiz:{ question:'What does 17 % 5 return in Python?', options:['3','2','3.4','0'], answer:1, explanation:'17 % 5 gives the remainder when 17 is divided by 5. 5×3=15, and 17−15=2. So the answer is 2.' },
      challenge:{ type:'code', title:'The Calculator', task:'Given a = 15 and b = 4, print exactly these 3 lines:\n19\n3\n50625', expectedOutput:'19\n3\n50625', hint:'print(a + b), then print(a % b), then print(a ** b).', starterCode:'a = 15\nb = 4\n# Print a + b\n\n# Print a % b\n\n# Print a ** b\n', validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l.includes('19')&&l.includes('3')&&l.includes('50625'); } },
    },
    {
      id:'py-b-4', title:'Strings In Depth', xp:40,
      timestamps:[
        { time:'0:00', label:'Indexing characters', content:'Every character in a string has an index starting at 0. Negative indexes count from the end — -1 is always the last character, no matter how long the string is.', bullets:['text[0] → first character','text[-1] → last character','Accessing a non-existent index raises IndexError'], snippet:'word = "Python"\nprint(word[0])   # P\nprint(word[1])   # y\nprint(word[-1])  # n' },
        { time:'1:45', label:'Slicing', content:'Slicing extracts a portion using [start:end]. The end index is NOT included. A third argument sets the step — [::2] skips every other character, [::-1] reverses the string.', bullets:['text[0:5] → characters at positions 0,1,2,3,4','text[:5] → same as [0:5]','text[::-1] → the entire string reversed'], snippet:'s = "Hello, World!"\nprint(s[0:5])    # Hello\nprint(s[7:])     # World!\nprint(s[::-1])   # !dlroW ,olleH' },
        { time:'3:20', label:'f-strings', content:'f-strings embed variables directly inside strings. Put f before the quote and wrap variables in curly braces. Any valid Python expression works inside the braces — not just variable names.', bullets:['f"..." ← the f before the quote activates it','f"Hello {name}!" ← name replaced with its value','f"Next year: {age + 1}" ← maths inside the braces'], snippet:'name = "Alice"\nage = 25\nprint(f"My name is {name}.")\nprint(f"I am {age} years old.")\nprint(f"Next year: {age + 1}")' },
        { time:'5:00', label:'Essential string methods', content:'String methods are built-in functions that operate on a string and return a new one. Strings are immutable — methods never change the original, they always return a new value.', bullets:['.upper() → all capitals','.strip() → removes spaces from both ends','.replace("old","new") → swaps every occurrence'], snippet:'text = "  hello world  "\nprint(text.strip())           # "hello world"\nprint(text.strip().upper())    # "HELLO WORLD"\nprint("a,b,c".split(","))      # [\'a\',\'b\',\'c\']' },
        { time:'7:10', label:'String immutability', content:'You cannot change a character inside a string directly — text[0] = "H" causes a TypeError. Instead, use replace() or build a new string, then reassign the variable.', bullets:['Strings are immutable — characters cannot be changed in place','Every method returns a NEW string','word = word.upper() re-assigns the variable to the new string'], snippet:'text = "hello"\n# text[0] = "H"  ← TypeError!\ntext = text.capitalize()  # creates new string\nprint(text)  # Hello' },
      ],
      codePanel:{
        explanation:'These are the most important string operations in real code. Each line is self-contained — the comment tells you what it does and what you\'ll see as output when you run it.',
        lines:[
          { comment:'First character (index 0)', code:'print("Python"[0])           # P' },
          { comment:'Slice — positions 0,1,2,3,4 (not 5)', code:'print("Hello World"[0:5])    # Hello' },
          { comment:'f-string — embed variable in text', code:'name = "Alice"\nprint(f"Hello, {name}!")' },
          { comment:'Convert to all capitals', code:'print("hello".upper())       # HELLO' },
          { comment:'Remove spaces on both ends', code:'print("  hi  ".strip())      # hi' },
          { comment:'Swap one word for another', code:'print("I like cats".replace("cats","dogs"))' },
        ],
      },
      quiz:{ question:'What does "Python"[1:4] return?', options:['"Pyt"','"yth"','"ytho"','"ython"'], answer:1, explanation:'[1:4] starts at index 1 (y) and goes up to but NOT including index 4 (o). Positions 1,2,3 are y,t,h — giving "yth".' },
      challenge:{ type:'code', title:'String Manipulator', task:'Given sentence = "the quick brown fox"\nPrint EXACTLY:\nTHE QUICK BROWN FOX\nthe quick\nthe quick brown dog', expectedOutput:'THE QUICK BROWN FOX\nthe quick\nthe quick brown dog', hint:'Use .upper(), [:9], and .replace("fox","dog").', starterCode:'sentence = "the quick brown fox"\n# 1. ALL CAPS\n\n# 2. First 9 characters\n\n# 3. Replace fox with dog\n', validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l.includes('THE QUICK BROWN FOX')&&l.includes('the quick')&&l.includes('the quick brown dog'); } },
    },
    {
      id:'py-b-5', title:'Lists', xp:40,
      timestamps:[
        { time:'0:00', label:'What is a list?', content:'A list is an ordered, mutable collection stored in one variable. Unlike strings, lists can hold items of completely different types and you can change them after creation.', bullets:['Defined with square brackets: [item1, item2, item3]','Ordered — items always stay in the position you put them','Mutable — you can change, add, or remove items anytime'], snippet:'fruits = ["apple","banana","cherry"]\nnumbers = [1, 2, 3, 4, 5]\nmixed = [42, "hello", True, 3.14]' },
        { time:'1:15', label:'Accessing items', content:'Access list items using their index — exactly like strings. Index starts at 0. Negative indexes count from the end. Slicing works the same way too.', bullets:['fruits[0] → first item','fruits[-1] → last item','fruits[1:3] → slice from index 1 to 2 (not 3)'], snippet:'fruits = ["apple","banana","cherry"]\nprint(fruits[0])    # apple\nprint(fruits[-1])   # cherry\nprint(fruits[1:3])  # [\'banana\',\'cherry\']' },
        { time:'2:50', label:'append, insert, remove', content:'These three methods modify a list. append() adds to the end (most common). insert() adds at a specific position. remove() deletes the first matching item by value.', bullets:['fruits.append("mango") → adds at the END','fruits.insert(1,"grape") → inserts at index 1, shifts rest right','fruits.remove("banana") → removes FIRST occurrence'], snippet:'fruits = ["apple","banana"]\nfruits.append("cherry")    # end\nfruits.insert(1,"grape")   # at index 1\nfruits.remove("banana")    # remove by value\nprint(fruits)' },
        { time:'4:30', label:'pop and len', content:'pop() removes and returns the last item (or item at a given index). len() returns the total number of items in the list.', bullets:['fruits.pop() → removes the last item AND returns it','fruits.pop(0) → removes the item at index 0','len(fruits) → count of items in the list'], snippet:'fruits = ["apple","banana","cherry"]\nremoved = fruits.pop()\nprint(removed)         # cherry\nprint(len(fruits))     # 2' },
        { time:'5:45', label:'Useful list methods', content:'Python lists have built-in methods for sorting, searching and counting that save you from writing loops manually.', bullets:['nums.sort() → sorts IN PLACE (modifies original)','sorted(nums) → returns NEW sorted list (original unchanged)','nums.count(5) → how many times 5 appears'], snippet:'nums = [3,1,4,1,5,9,2,6]\nnums.sort()\nprint(nums)       # [1,1,2,3,4,5,6,9]\nprint(min(nums))  # 1\nprint(max(nums))  # 9\nprint(sum(nums))  # 31' },
        { time:'7:00', label:'Nested lists', content:'A list can contain other lists as items — creating a 2D structure called a matrix. Access inner items with two indexes: outer[row][col].', bullets:['matrix[0] → the first inner list','matrix[1][2] → row 1, column 2','Used for grids, tables, and game boards'], snippet:'matrix = [[1,2,3],[4,5,6],[7,8,9]]\nprint(matrix[0])      # [1,2,3]\nprint(matrix[1][2])   # 6' },
      ],
      codePanel:{
        explanation:'Lists are the most commonly used data structure in Python. Each line demonstrates a different operation — notice how append() changes the list while len() just reads it.',
        lines:[
          { comment:'Create a list with 3 items', code:'fruits = ["apple","banana","cherry"]' },
          { comment:'Get the first item (index 0)', code:'print(fruits[0])          # apple' },
          { comment:'Add a new item to the end', code:'fruits.append("mango")' },
          { comment:'Remove and return the last item', code:'print(fruits.pop())       # mango' },
          { comment:'Count how many items remain', code:'print(len(fruits))        # 3' },
        ],
      },
      quiz:{ question:'Which method adds an item to the END of a list?', options:['add()','insert()','append()','push()'], answer:2, explanation:'append() adds a single item to the end. insert(index, item) adds at a specific position. Python has no push() — that\'s JavaScript!' },
      challenge:{ type:'code', title:'Shopping Cart', task:'Start with cart = []. Add "milk", "eggs", "bread" using append(). Then print the full list and its length on separate lines.\nExpected:\n[\'milk\', \'eggs\', \'bread\']\n3', expectedOutput:"['milk', 'eggs', 'bread']\n3", hint:'Three append() calls, then print(cart), then print(len(cart)).', starterCode:"cart = []\n# Add the 3 items\n\n\n\n# Print the list\n\n# Print the length\n", validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l.some(x=>x.includes('milk')&&x.includes('eggs')&&x.includes('bread'))&&l.includes('3'); } },
    },
    {
      id:'py-b-6', title:'Conditionals — if/elif/else', xp:40,
      timestamps:[
        { time:'0:00', label:'Making decisions in code', content:'Every program needs to make decisions — do this if X, otherwise do that. Conditionals are how Python chooses which block of code to execute based on whether a condition is True or False.', bullets:['Conditions are expressions that evaluate to True or False','Python uses indentation (4 spaces) to define blocks','Every if-statement ends with a colon :'], snippet:null },
        { time:'1:00', label:'The if statement', content:'The indented block ONLY runs if the condition is True. If False, Python skips the entire block and continues with the next unindented line.', bullets:['if condition: ← always ends with colon','    indented code ← only runs if True','Code at same level as if ← always runs'], snippet:'age = 20\nif age >= 18:\n    print("You are an adult")\n    print("You can vote!")\nprint("This always runs")' },
        { time:'2:30', label:'if-else — two paths', content:'else gives a fallback path. If the condition is True, the if block runs. If False, the else block runs. Exactly ONE of them always runs.', bullets:['else has no condition — it\'s the "everything else" case','Must be at the same indentation as its if','Can\'t have else without an if first'], snippet:'temp = 15\nif temp > 25:\n    print("Hot — wear a t-shirt!")\nelse:\n    print("Cool — bring a jacket!")' },
        { time:'4:00', label:'if-elif-else', content:'elif lets you check multiple conditions in sequence. Python checks top to bottom and runs the FIRST one that\'s True — then skips all the rest.', bullets:['You can have as many elif blocks as you need','Only the FIRST matching branch runs','The final else is optional but acts as a catch-all'], snippet:'score = 72\nif score >= 90:\n    grade = "A"\nelif score >= 80:\n    grade = "B"\nelif score >= 70:\n    grade = "C"\nelse:\n    grade = "F"\nprint(f"Grade: {grade}")' },
        { time:'5:45', label:'Nested conditions', content:'You can put an if-statement inside another if-statement. Use it when a second check only makes sense after the first one passes.', bullets:['Each level adds another 4 spaces of indentation','Don\'t nest too deeply — use and/or instead where possible','Keep nesting to 2 levels max for readability'], snippet:'is_member = True\nbalance = 150\nif is_member:\n    if balance >= 100:\n        print("Approved!")\n    else:\n        print("Low balance")\nelse:\n    print("Please sign up")' },
        { time:'7:00', label:'Ternary — one-liner', content:'Python\'s ternary expression writes a simple if-else on a single line. Great for assigning a value based on a condition without a full block.', bullets:['value_if_true if condition else value_if_false','Use only for simple cases','status = "adult" if age >= 18 else "minor"'], snippet:'age = 20\nstatus = "adult" if age >= 18 else "minor"\nprint(status)   # adult\n\nlabel = "even" if 8 % 2 == 0 else "odd"\nprint(label)    # even' },
      ],
      codePanel:{
        explanation:'Conditionals let your program take different paths. These lines show a grade calculator — each elif is checked in order, and only the first matching one runs. Read the comments to follow the logic.',
        lines:[
          { comment:'The score we want to evaluate', code:'score = 72' },
          { comment:'First check — is it 90+?', code:'if score >= 90:\n    grade = "A"' },
          { comment:'Second check — runs only if first was False', code:'elif score >= 70:\n    grade = "C"  # runs because 72 >= 70' },
          { comment:'Fallback — runs if nothing above matched', code:'else:\n    grade = "F"' },
          { comment:'Print the result', code:'print(f"Grade: {grade}")  # Grade: C' },
        ],
      },
      quiz:{ question:'If score = 85: which branch runs in if score>=90 / elif score>=80 / else?', options:['The if block','The elif block','The else block','Both if and elif'], answer:1, explanation:'85 is NOT ≥ 90 → if skipped. 85 IS ≥ 80 → elif runs. Python stops here. Only the FIRST True branch executes.' },
      challenge:{ type:'code', title:'Grade Calculator', task:'Given score = 78, print ONLY the letter grade:\n- A (90+), B (80+), C (70+), D (60+), F (below 60)\nOutput must be exactly: C', expectedOutput:'C', hint:'Use if/elif/elif/elif/else and print only the single letter.', starterCode:'score = 78\n# your if/elif/else here\n', validate:(output) => output.trim() === 'C' },
    },
    {
      id:'py-b-7', title:'Loops — for & while', xp:40,
      timestamps:[
        { time:'0:00', label:'Why loops exist', content:'Without loops, you\'d write print() a thousand times to print a thousand numbers. Loops repeat a block of code automatically — either a fixed number of times or until a condition changes.', bullets:['for loops: best when you know how many repetitions','while loops: best when you repeat until something happens','Loops are the most powerful tool for processing data'], snippet:null },
        { time:'1:00', label:'for loop with range()', content:'The for loop with range() repeats code a specific number of times. range(n) generates numbers 0 to n-1. range(start, stop, step) gives you full control over the sequence.', bullets:['range(5) → 0,1,2,3,4  (5 numbers from 0)','range(1,6) → 1,2,3,4,5','range(0,10,2) → 0,2,4,6,8  (step of 2)'], snippet:'for i in range(5):\n    print(i)       # 0 1 2 3 4\n\nfor i in range(1, 4):\n    print(i)       # 1 2 3' },
        { time:'3:00', label:'Iterating over lists', content:'The for loop can iterate directly over any sequence — a list, string, or collection. Python gives you each item one at a time, no index needed.', bullets:['for fruit in fruits: gives each fruit directly','for char in "hello": gives h, e, l, l, o one by one','for i, item in enumerate(list): gives index AND item'], snippet:'fruits = ["apple","banana","cherry"]\nfor fruit in fruits:\n    print(f"I like {fruit}")\n\nfor i, fruit in enumerate(fruits):\n    print(f"{i}: {fruit}")' },
        { time:'4:30', label:'while loop', content:'The while loop keeps running as long as its condition is True. Unlike for, use it when you don\'t know in advance how many repetitions you need.', bullets:['while condition: ← runs while condition is True','Always make the condition eventually False — or infinite loop!','count += 1 inside the loop makes progress toward stopping'], snippet:'count = 0\nwhile count < 5:\n    print(f"Count: {count}")\n    count += 1\nprint("Done")' },
        { time:'6:00', label:'break and continue', content:'break exits the loop immediately. continue skips the rest of the current iteration and jumps to the next one. Both work inside for AND while loops.', bullets:['break: stop the entire loop right now','continue: skip this iteration, keep going','Use sparingly — loops are cleaner without them when possible'], snippet:'for i in range(10):\n    if i == 6:\n        break       # stop at 6\n    if i % 2 == 0:\n        continue    # skip even numbers\n    print(i)        # prints 1 3 5' },
        { time:'7:30', label:'enumerate() and zip()', content:'enumerate() gives both the index and value when looping. zip() lets you loop over two lists simultaneously, pairing items by position.', bullets:['enumerate(list) → (index, value) pairs','zip(list1, list2) → pairs items at same position','Both are efficient — they don\'t create extra lists in memory'], snippet:'names = ["Alice","Bob","Carol"]\nscores = [95, 82, 78]\nfor name, score in zip(names, scores):\n    print(f"{name}: {score}")' },
      ],
      codePanel:{
        explanation:'Three essential loop patterns you\'ll use constantly. Each block is independent — study the comments to understand what each loop does and how it terminates.',
        lines:[
          { comment:'Count 1 to 5 using range', code:'for i in range(1, 6):\n    print(i)' },
          { comment:'Visit every item in a list', code:'for fruit in ["apple","banana"]:\n    print(fruit)' },
          { comment:'While loop — stops when count reaches 3', code:'count = 0\nwhile count < 3:\n    print(count)\n    count += 1' },
        ],
      },
      quiz:{ question:'What does range(2, 10, 3) produce?', options:['2, 5, 8','2,3,4,5,6,7,8,9','3,6,9','2,4,6,8'], answer:0, explanation:'Starts at 2, step 3 each time: 2 → 5 → 8 → 11. 11 ≥ 10 so it stops. Result: 2, 5, 8.' },
      challenge:{ type:'code', title:'Multiplication Table', task:'Print the 7 times table from 7×1 to 7×5. Each line must be in this exact format:\n7 x 1 = 7\n7 x 2 = 14\n...', expectedOutput:'7 x 1 = 7\n7 x 2 = 14\n7 x 3 = 21\n7 x 4 = 28\n7 x 5 = 35', hint:'for i in range(1,6): then print(f"7 x {i} = {7*i}")', starterCode:'for i in range(1, 6):\n    pass  # replace with your print\n', validate:(output) => { const e=['7 x 1 = 7','7 x 2 = 14','7 x 3 = 21','7 x 4 = 28','7 x 5 = 35']; const l=output.trim().split('\n').map(x=>x.trim()); return e.every(x=>l.includes(x)); } },
    },
    {
      id:'py-b-8', title:'Functions', xp:40,
      timestamps:[
        { time:'0:00', label:'Why functions?', content:'Functions let you write a block of code once and reuse it everywhere. Without functions, every program would be a long sequence of repeated, unmaintainable code.', bullets:['Write once, call anywhere','Fix a bug in the function → it\'s fixed everywhere','Good function names make code self-documenting'], snippet:null },
        { time:'1:15', label:'def — defining a function', content:'Use the def keyword, followed by the function name, parentheses, and a colon. The indented block below is the function\'s body. The function does nothing until it\'s called.', bullets:['def function_name(): ← the definition','function_name() ← the call','A function does nothing until explicitly called'], snippet:'def greet():\n    print("Hello! Welcome to Python!")\n\ngreet()   # first call\ngreet()   # second call — runs body again' },
        { time:'2:45', label:'Parameters and arguments', content:'Parameters are the variable names in the definition. Arguments are the actual values passed when calling. Default values let a parameter be optional.', bullets:['def greet(name): ← name is the parameter','greet("Alice") ← "Alice" is the argument','Default: def greet(name="World") ← used when no arg given'], snippet:'def greet(name, greeting="Hello"):\n    print(f"{greeting}, {name}!")\n\ngreet("Alice")              # Hello, Alice!\ngreet("Bob", "Good morning") # Good morning, Bob!' },
        { time:'4:20', label:'The return statement', content:'return sends a value back to the caller and exits the function immediately. Without return, the function returns None by default.', bullets:['return exits the function immediately','The returned value can be stored: result = add(3,4)','A function can return different values based on conditions'], snippet:'def add(a, b):\n    return a + b\n\nresult = add(10, 5)\nprint(result)       # 15\nprint(add(3.14, 2)) # 5.14' },
        { time:'5:50', label:'Multiple return values', content:'Python functions can return multiple values separated by commas. Python packs them into a tuple, which you can unpack into separate variables on the left side of =.', bullets:['return a, b ← returns both together as a tuple','lo, hi = func() ← unpacks into two variables','Useful for functions that compute multiple related results'], snippet:'def min_max(numbers):\n    return min(numbers), max(numbers)\n\nlo, hi = min_max([3,1,9,2,7])\nprint(f"Min: {lo}, Max: {hi}")' },
        { time:'7:10', label:'Scope', content:'Variables created inside a function are local — they exist only while the function runs. Variables outside are global — readable everywhere but modifying them inside requires the global keyword.', bullets:['Local variables disappear when the function ends','Global variables can be READ inside functions freely','Avoid global where possible — it makes code hard to test'], snippet:'x = 10  # global\n\ndef my_func():\n    y = 20   # local\n    print(x)  # can read global\n\nmy_func()\n# print(y)  ← NameError! y is gone' },
      ],
      codePanel:{
        explanation:'This function takes two numbers, adds them, and returns the result. Each line is labelled — follow the flow from definition to call to output.',
        lines:[
          { comment:'Define: function that takes two inputs', code:'def add(a, b):' },
          { comment:'Compute the sum and send it back to caller', code:'    return a + b' },
          { comment:'Call the function and store what it returns', code:'result = add(10, 5)' },
          { comment:'Print the stored result', code:'print(result)           # 15' },
          { comment:'Or use the return value directly', code:'print(add(3, 7))        # 10' },
        ],
      },
      quiz:{ question:'What does a function return if there is no return statement?', options:['0','""  (empty string)','None','An error is raised'], answer:2, explanation:'Every Python function returns something. Without a return statement, Python automatically returns None — a special value meaning "no value".' },
      challenge:{ type:'code', title:'is_even Function', task:'Write a function called is_even(n) that returns True if n is even, False otherwise.\nThen call it:\nprint(is_even(4))  → True\nprint(is_even(7))  → False', expectedOutput:'True\nFalse', hint:'Return n % 2 == 0 — this expression IS True or False already.', starterCode:'def is_even(n):\n    pass  # replace with return statement\n\nprint(is_even(4))\nprint(is_even(7))\n', validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l[0]==='True'&&l[1]==='False'; } },
    },
  ],
  intermediate: [
    {
      id:'py-i-1', title:'Dictionaries', xp:40,
      timestamps:[
        { time:'0:00', label:'What is a dictionary?', content:'A dictionary stores data as key-value pairs — like a real dictionary where each word (key) maps to a definition (value). Keys must be unique and immutable; values can be anything.', bullets:['Defined with curly braces: {"key": value}','Keys are usually strings but can be ints or tuples','Maintains insertion order since Python 3.7'], snippet:'person = {"name":"Alice","age":25}\nprint(person)' },
        { time:'1:30', label:'Accessing values', content:'Access a value using its key in square brackets. If the key doesn\'t exist, you get a KeyError. Use .get() for a safe lookup that returns None instead of crashing.', bullets:['person["name"] → "Alice"','person["missing"] → KeyError crash','person.get("missing") → None (safe)'], snippet:'person = {"name":"Alice","age":25}\nprint(person["name"])            # Alice\nprint(person.get("email","N/A")) # N/A' },
        { time:'3:00', label:'Adding and updating', content:'Assign to a key to update it if it exists or create it if new. del removes a key entirely. .pop() removes and returns the value.', bullets:['person["age"] = 26 → updates existing key','person["email"] = "a@b.com" → adds new key','del person["age"] → removes the key entirely'], snippet:'person = {"name":"Alice"}\nperson["age"] = 25\nperson["name"] = "Bob"\ndel person["age"]\nprint(person)' },
        { time:'4:30', label:'Iterating', content:'.keys() loops over keys. .values() over values. .items() gives both as key-value tuples — the most useful method for iterating over all data in a dictionary.', bullets:['for k in d: → keys only','for v in d.values(): → values only','for k,v in d.items(): → both together'], snippet:'scores = {"Alice":95,"Bob":82}\nfor name, score in scores.items():\n    print(f"{name}: {score}")' },
        { time:'6:00', label:'Nested dictionaries', content:'Dictionary values can be other dictionaries — creating a nested structure ideal for complex data like user profiles, API responses, and configuration objects.', bullets:['Access nested: data["user"]["age"]','json.loads() converts JSON into nested Python dicts','Common pattern for records of records'], snippet:'users = {\n  "alice": {"age":25,"score":95},\n  "bob":   {"age":30,"score":82},\n}\nprint(users["alice"]["score"])  # 95' },
      ],
      codePanel:{ explanation:'The five most common dictionary operations in one place — create, read safely, add, loop, and nest. Each line has a label so you know exactly what\'s happening.', lines:[{comment:'Create with key-value pairs', code:'person = {"name":"Alice","age":25}'},{comment:'Safe read — no crash if key missing', code:'print(person.get("email","N/A"))'},{comment:'Add a new key', code:'person["city"] = "Mumbai"'},{comment:'Loop over all pairs', code:'for k,v in person.items():\n    print(k,v)'},{comment:'Nested — dict inside a dict', code:'data = {"user": {"age": 25}}\nprint(data["user"]["age"])  # 25'}] },
      quiz:{ question:'What does dict.get("missing_key") return if the key doesn\'t exist?', options:['Raises KeyError','0','False','None'], answer:3, explanation:'.get() returns None by default when a key is missing — making it the safe alternative to dict["key"] which would crash with a KeyError.' },
      challenge:{ type:'code', title:'Student Record', task:'Create a dictionary called student with keys "name", "grade", and "age".\nThen print each key: value pair on its own line.\nExample:\nname: Alice\ngrade: A\nage: 20', expectedOutput:null, hint:'Loop with for key, value in student.items(): then print(f"{key}: {value}")', starterCode:'student = {\n    "name": "Alice",\n    "grade": "A",\n    "age": 20\n}\nfor key, value in student.items():\n    print(f"{key}: {value}")\n', validate:(output) => output.includes(':')&&output.trim().split('\n').length>=3 },
    },
    {
      id:'py-i-2', title:'Classes & OOP', xp:40,
      timestamps:[
        { time:'0:00', label:'What is OOP?', content:'Object-Oriented Programming organises code around objects — bundles of data (attributes) and behaviour (methods). A class is the blueprint; an object is a concrete instance built from it.', bullets:['Class = blueprint (e.g. Dog)','Object = specific instance (e.g. my dog "Buddy")','Each object gets its own independent copy of the attributes'], snippet:null },
        { time:'1:30', label:'Defining a class', content:'Use the class keyword followed by the class name (CapitalCase convention). The body contains attributes and methods, all indented under the class.', bullets:['class Dog: ← defines the class','Methods are functions defined inside a class','First parameter of every method must be self'], snippet:'class Dog:\n    def bark(self):\n        print("Woof!")\n\nd = Dog()\nd.bark()   # Woof!' },
        { time:'3:15', label:'__init__ constructor', content:'__init__ runs automatically whenever you create a new object. It initialises the object\'s attributes using the arguments you pass in.', bullets:['__init__ runs on Dog("Buddy","Lab")','self.name = name stores the argument as an attribute','Every object gets its own independent copy'], snippet:'class Dog:\n    def __init__(self, name, breed):\n        self.name = name\n        self.breed = breed\n\nd = Dog("Buddy","Labrador")\nprint(d.name)    # Buddy' },
        { time:'5:00', label:'Methods', content:'Methods are functions inside a class that use self to access the object\'s own data. Each object has the same methods but operates on its own attribute values.', bullets:['def method(self): ← always takes self first','self.attribute accesses this object\'s data','Call with object.method() — Python passes self automatically'], snippet:'class Dog:\n    def __init__(self, name):\n        self.name = name\n    def bark(self):\n        return f"{self.name} says: Woof!"\n\nd = Dog("Rex")\nprint(d.bark())   # Rex says: Woof!' },
        { time:'6:30', label:'Inheritance', content:'A child class reuses all methods and attributes from a parent class. super().__init__() runs the parent\'s constructor first, then the child adds its own extras.', bullets:['class Child(Parent): ← inherits everything from Parent','super().__init__() ← run Parent\'s __init__ first','Child can override any parent method by redefining it'], snippet:'class Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def speak(self):\n        return f"{self.name}: Woof!"\n\nd = Dog("Rex")\nprint(d.speak())  # Rex: Woof!' },
      ],
      codePanel:{ explanation:'This BankAccount class shows how attributes store state and methods change it. Notice how two objects are completely independent — Alice\'s deposits don\'t affect Bob\'s balance.', lines:[{comment:'Define class with constructor', code:'class BankAccount:\n    def __init__(self, owner, balance=0):\n        self.owner = owner\n        self.balance = balance'},{comment:'Method that modifies the object\'s state', code:'    def deposit(self, amount):\n        self.balance += amount'},{comment:'Create two independent objects', code:'acc1 = BankAccount("Alice", 1000)\nacc2 = BankAccount("Bob")'},{comment:'Changes to acc1 don\'t affect acc2', code:'acc1.deposit(500)\nprint(acc1.balance)   # 1500\nprint(acc2.balance)   # 0'}] },
      quiz:{ question:'What is the purpose of __init__ in a Python class?', options:['Called when object is deleted','Initialises attributes when object is created','Required but has no special purpose','Makes class inherit from another'], answer:1, explanation:'__init__ is the constructor — it runs automatically every time you create a new object (e.g. Dog("Buddy")). It sets up the object\'s initial state.' },
      challenge:{ type:'code', title:'Counter Class', task:'Create a class Counter with:\n- count starting at 0 in __init__\n- increment() adds 1 to count\n- get() returns count\nCreate a Counter, call increment() twice, print get().\nOutput must be exactly: 2', expectedOutput:'2', hint:'self.count = 0 in __init__. increment does self.count += 1. get returns self.count.', starterCode:'class Counter:\n    def __init__(self):\n        self.count = 0\n    def increment(self):\n        pass  # add 1\n    def get(self):\n        pass  # return count\n\nc = Counter()\nc.increment()\nc.increment()\nprint(c.get())\n', validate:(output) => output.trim()==='2' },
    },
    {
      id:'py-i-3', title:'File Handling & Exceptions', xp:40,
      timestamps:[
        { time:'0:00', label:'Reading files', content:'open() opens a file and returns a file object. Always use with open() — it automatically closes the file when the block ends, even if an error occurs.', bullets:['with open("file.txt","r") as f: ← safest approach','f.read() → entire file as one string','f.readlines() → list of lines'], snippet:'with open("notes.txt","r") as f:\n    content = f.read()\n    print(content)' },
        { time:'2:15', label:'Writing files', content:'"w" creates or overwrites. "a" appends to the end without deleting. Always use with open() to ensure the file is properly saved and closed.', bullets:['"w" overwrites everything — be careful!','"a" appends — safe to use repeatedly','"x" creates new file — error if already exists'], snippet:'with open("notes.txt","w") as f:\n    f.write("First line\\n")\n    f.write("Second line\\n")' },
        { time:'3:45', label:'try / except', content:'try/except handles errors gracefully. Code that might fail goes in try. The except block only runs if a specific error occurs — your program continues instead of crashing.', bullets:['try: ← risky code goes here','except ValueError: ← catches only ValueError','Multiple except blocks handle different error types'], snippet:'try:\n    num = int("not a number")\nexcept ValueError as e:\n    print(f"Error: {e}")' },
        { time:'5:30', label:'Multiple exception types', content:'Separate except blocks handle different errors differently. This lets you give tailored responses to each failure mode rather than one generic error message.', bullets:['except ZeroDivisionError: ← catches 10/0','except TypeError: ← wrong data type','except Exception: ← catches anything (use as last resort)'], snippet:'def divide(a, b):\n    try:\n        return a / b\n    except ZeroDivisionError:\n        return "Cannot divide by zero"\n    except TypeError:\n        return "Numbers only please"' },
        { time:'6:45', label:'finally', content:'The finally block ALWAYS runs — whether the try succeeded or not. Use it for cleanup that must always happen: closing resources, logging, resetting state.', bullets:['finally: runs no matter what happens','Runs even if except re-raises an error','Common use: close files, release database connections'], snippet:'try:\n    result = 10 / 2\nexcept ZeroDivisionError:\n    print("Error!")\nfinally:\n    print("This always runs")' },
      ],
      codePanel:{ explanation:'Exception handling stops crashes. These lines show the full pattern — try wraps risky code, each except handles a specific error type, finally always cleans up.', lines:[{comment:'Risky code goes inside try', code:'try:'},{comment:'This might fail if b is 0', code:'    result = 10 / 0'},{comment:'Runs ONLY if ZeroDivisionError occurs', code:'except ZeroDivisionError:'},{comment:'Handle it gracefully', code:'    result = None\n    print("Can\'t divide by zero")'},{comment:'ALWAYS runs — success or failure', code:'finally:\n    print("Attempt done")'}] },
      quiz:{ question:'Which block ALWAYS runs regardless of success or failure?', options:['try','except','else','finally'], answer:3, explanation:'finally always executes — even if try succeeded, even if except raised another error. It\'s used for cleanup like closing files and releasing resources.' },
      challenge:{ type:'code', title:'Safe Divider', task:'Write safe_divide(a, b) that:\n- Returns a/b normally\n- Returns "Error: divide by zero" if b is 0\n\nprint(safe_divide(10, 2))  → 5.0\nprint(safe_divide(10, 0))  → Error: divide by zero', expectedOutput:'5.0\nError: divide by zero', hint:'try: return a/b  except ZeroDivisionError: return "Error: divide by zero"', starterCode:'def safe_divide(a, b):\n    try:\n        pass  # return a/b\n    except ZeroDivisionError:\n        pass  # return the error string\n\nprint(safe_divide(10, 2))\nprint(safe_divide(10, 0))\n', validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l[0]==='5.0'&&l[1]==='Error: divide by zero'; } },
    },
  ],
  expert: [
    {
      id:'py-e-1', title:'Decorators', xp:40,
      timestamps:[
        { time:'0:00', label:'First-class functions', content:'In Python, functions are objects. You can assign them to variables, pass them as arguments to other functions, and return them from functions — exactly like integers or strings.', bullets:['greet = say_hello ← assign function to a variable','run(say_hello) ← pass function as argument','Functions that take/return functions are higher-order functions'], snippet:'def say_hi():\n    return "Hi!"\n\nfn = say_hi\nprint(fn())    # Hi!' },
        { time:'2:00', label:'Closures', content:'A closure is an inner function that remembers variables from its enclosing outer function\'s scope — even after the outer function has finished running.', bullets:['The inner function "closes over" the outer variable','This mechanism makes decorators possible','Used for factory functions and stateful callbacks'], snippet:'def outer(msg):\n    def inner():\n        print(msg)  # remembers msg\n    return inner\n\ngreet = outer("Hello!")\ngreet()   # Hello!' },
        { time:'4:00', label:'Building a decorator', content:'A decorator wraps a function to add extra behaviour without modifying the original code. It takes a function, wraps it in another function, and returns the wrapper.', bullets:['wrapper(*args, **kwargs) ← accepts any arguments','Calls the original func inside','Returns the wrapper as the replacement'], snippet:'def my_decorator(func):\n    def wrapper(*args, **kwargs):\n        print("Before")\n        result = func(*args, **kwargs)\n        print("After")\n        return result\n    return wrapper' },
        { time:'6:00', label:'The @ syntax', content:'The @ symbol is syntactic sugar — @timer before a function is exactly equivalent to func = timer(func) written right after the definition. Cleaner and standard.', bullets:['@decorator above def applies it automatically','Multiple decorators stack: @A @B def f() → A(B(f))','functools.wraps preserves original function\'s __name__'], snippet:'@my_decorator\ndef say_hello():\n    print("Hello!")\n\nsay_hello()\n# Before\n# Hello!\n# After' },
        { time:'7:30', label:'Practical decorators', content:'Real decorators power major frameworks. Flask\'s @app.route() for URL routing, Django\'s @login_required for auth, and common patterns like timing, logging, and caching.', bullets:['@timer: measure how long a function takes','@logger: record every function call and args','@cache: store return values to avoid repeating expensive work'], snippet:'import time, functools\ndef timer(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        t = time.time()\n        result = func(*args, **kwargs)\n        print(f"{func.__name__} took {time.time()-t:.4f}s")\n        return result\n    return wrapper' },
      ],
      codePanel:{ explanation:'The timer decorator measures how long any function takes — without touching the function\'s own code. @functools.wraps preserves the original name so debugging stays clean.', lines:[{comment:'Decorator receives the function to wrap', code:'def timer(func):'},{comment:'wrapper adds behaviour around the call', code:'    def wrapper(*args, **kwargs):'},{comment:'Record time before running', code:'        start = time.time()'},{comment:'Run the original function', code:'        result = func(*args, **kwargs)'},{comment:'Print elapsed time', code:'        print(f"Took {time.time()-start:.3f}s")\n        return result\n    return wrapper'},{comment:'Apply with clean @ syntax', code:'@timer\ndef slow_func():\n    time.sleep(0.1)'}] },
      quiz:{ question:'With @A on top of @B on function f — which wrapper\'s code runs first when you CALL f?', options:['A\'s code first (outermost)','B\'s code first (closest to function)','Both simultaneously','Order doesn\'t matter'], answer:0, explanation:'Decorators apply bottom-up (B wraps f first, then A wraps that). But when CALLED, A\'s wrapper runs first (outermost), then calls B\'s wrapper, which calls original f.' },
      challenge:{ type:'code', title:'Logger Decorator', task:'Write a decorator called logger that prints "Calling: function_name" before running any function.\nApply it to add(a,b) that returns a+b.\ncall add(3,4) — output must be exactly:\nCalling: add\n7', expectedOutput:'Calling: add\n7', hint:'import functools. Inside wrapper: print(f"Calling: {func.__name__}") then return func(*args, **kwargs)', starterCode:'import functools\n\ndef logger(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        pass  # print "Calling: name"\n        pass  # call and return function\n    return wrapper\n\n@logger\ndef add(a, b):\n    return a + b\n\nprint(add(3, 4))\n', validate:(output) => { const l=output.trim().split('\n').map(x=>x.trim()); return l[0]==='Calling: add'&&l[1]==='7'; } },
    },
    {
      id:'py-e-2', title:'Generators & Async/Await', xp:40,
      timestamps:[
        { time:'0:00', label:'The memory problem', content:'Creating a list of 1 million numbers loads all of them into RAM at once. For huge datasets, this wastes memory and can crash programs. Generators produce values one at a time — solving this.', bullets:['List of 1M numbers → ~8MB in RAM','Generator for same data → ~200 bytes','Generators are lazy — they compute only when asked'], snippet:'import sys\nbig_list = [x for x in range(100_000)]\nbig_gen  = (x for x in range(100_000))\nprint(sys.getsizeof(big_list))  # ~800KB\nprint(sys.getsizeof(big_gen))   # ~200 bytes' },
        { time:'1:30', label:'yield — the generator keyword', content:'A generator function uses yield instead of return. Each next() call runs until the next yield, pauses there, and hands back the value. State is preserved between calls.', bullets:['yield pauses the function and sends a value out','next(gen) resumes from the last yield','When function ends, StopIteration is raised'], snippet:'def count_up(n):\n    i = 0\n    while i < n:\n        yield i\n        i += 1\n\ngen = count_up(3)\nprint(next(gen))  # 0\nprint(next(gen))  # 1' },
        { time:'3:30', label:'Generator expressions', content:'Generator expressions use () instead of []. Same syntax as list comprehensions, but values are computed only when iterated — no memory wasted on items you haven\'t used yet.', bullets:['(expr for item in iterable) ← generator expression','list(gen_expr) ← forces all values at once','sum(x**2 for x in range(100)) ← no list created'], snippet:'squares_gen = (x**2 for x in range(5))\nfor sq in squares_gen:\n    print(sq)   # 0 1 4 9 16' },
        { time:'5:00', label:'What is async?', content:'Traditional code is synchronous — each line waits for the previous. Async code can START a slow operation (like a network request) and do other work while waiting, then come back when it\'s ready.', bullets:['Ideal for I/O-heavy code: APIs, databases, files','Python\'s asyncio event loop manages task switching','Async doesn\'t make code faster — it removes idle waiting time'], snippet:null },
        { time:'6:30', label:'async def and await', content:'An async function returns a coroutine that must be awaited. await pauses the current coroutine and hands control to the event loop until the operation completes — non-blocking.', bullets:['async def func(): ← creates a coroutine','await slow_op() ← non-blocking pause','asyncio.run(main()) ← entry point for async programs'], snippet:'import asyncio\n\nasync def greet(name, delay):\n    await asyncio.sleep(delay)\n    print(f"Hello, {name}!")\n\nasyncio.run(greet("Alice", 1))' },
        { time:'8:00', label:'asyncio.gather', content:'asyncio.gather() runs multiple coroutines concurrently. Total wait = slowest one (not the sum). Three 1-second tasks run in 1 second total, not 3.', bullets:['gather() starts all coroutines at once','Returns list of results in original order','If any raises, gather raises immediately'], snippet:'async def main():\n    r1, r2 = await asyncio.gather(\n        fetch("API-1", delay=1),\n        fetch("API-2", delay=2),\n    )\n    # Total wait: 2s not 3s!\n    print(r1, r2)' },
      ],
      codePanel:{ explanation:'A generator pipeline processes 20 numbers through three stages without ever creating a full list. Values flow through one at a time — memory usage stays tiny regardless of input size.', lines:[{comment:'Stage 1: yield numbers 1 to n', code:'def numbers(n):\n    for i in range(1, n+1): yield i'},{comment:'Stage 2: yield each value squared', code:'def squared(gen):\n    for v in gen: yield v ** 2'},{comment:'Stage 3: yield only even values', code:'def evens(gen):\n    for v in gen:\n        if v % 2 == 0: yield v'},{comment:'Chain all three — no lists ever created!', code:'pipe = evens(squared(numbers(20)))\nfor result in pipe:\n    print(result)'}] },
      quiz:{ question:'What is the key memory advantage of a generator over a list?', options:['Generators iterate faster','Generators store values for fast access','Generators produce one value at a time — only when needed','Generators hold more items than lists'], answer:2, explanation:'Generators use lazy evaluation — producing values on demand. A list of 1M items uses ~8MB; the equivalent generator uses ~200 bytes because it only holds the CURRENT value.' },
      challenge:{ type:'code', title:'Fibonacci Generator', task:'Write a generator function fibonacci() that yields Fibonacci numbers indefinitely.\nPrint the first 6:\n0\n1\n1\n2\n3\n5', expectedOutput:'0\n1\n1\n2\n3\n5', hint:'a, b = 0, 1. while True: yield a, then a, b = b, a+b', starterCode:'def fibonacci():\n    a, b = 0, 1\n    while True:\n        pass  # yield a, then update\n\nfib = fibonacci()\nfor _ in range(6):\n    print(next(fib))\n', validate:(output) => output.trim().split('\n').map(x=>x.trim()).join(',') === '0,1,1,2,3,5' },
    },
  ],
};
